import { products } from "../page";
import { SiteHeader } from "../site-header";
export const metadata={title:"Current Superbuy Spreadsheet Finds | Independent Product Index",description:"Browse current product records from the independent Superbuy spreadsheet and open each live listing for verification."};
export default function Finds(){return <main><SiteHeader/><section className="directory-shell"><p className="eyebrow">Current records</p><h1>Finds worth checking.</h1><p className="directory-intro">These records are discovery starting points, not endorsements. Check the exact option, seller information and current availability on the live destination.</p><div className="product-grid">{products.map(x=><a className="product-card" href={x.href} key={x.href}><div className="product-image-wrap"><img src={x.image} alt={x.name}/></div><div className="product-copy"><small>{x.category}</small><h3>{x.name}</h3><p>{x.check}</p><span>View record ↗</span></div></a>)}</div></section></main>}
