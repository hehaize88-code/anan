import { products } from "../page";
import { SiteHeader } from "../site-header";
import { BreadcrumbJsonLd, ItemListJsonLd, pageMetadata, VERIFIED_DATE_LABEL } from "../seo";
export const metadata=pageMetadata("Current Superbuy Spreadsheet Finds | Independent Product Index","Browse current product records from the independent Superbuy spreadsheet and open each live listing for verification.","/finds/");
export default function Finds(){return <main><BreadcrumbJsonLd items={[{name:"Home",path:"/"},{name:"Finds",path:"/finds/"}]}/><ItemListJsonLd name="Current Superbuy spreadsheet finds" path="/finds/" items={products.map(x=>({name:x.name,url:x.href}))}/><SiteHeader/><section className="directory-shell"><p className="eyebrow">Current records</p><h1>Finds worth checking.</h1><p className="directory-intro">These records are discovery starting points, not endorsements. Product links open our separately hosted catalogue in a new tab. Check the exact option, seller information and current availability before ordering.</p><div className="product-grid">{products.map(x=><a className="product-card" href={x.href} target="_blank" rel="noopener noreferrer" key={x.href}><div className="product-image-wrap"><img src={x.image} alt={x.name} width="720" height="720" loading="lazy" decoding="async"/></div><div className="product-copy"><small>{x.category}</small><h3>{x.name}</h3><p>{x.check}</p><div className="record-status"><span>Link active</span><span>Checked {VERIFIED_DATE_LABEL}</span></div><span>View external record ↗</span></div></a>)}</div></section></main>}
