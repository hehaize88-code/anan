import { categories } from "../page";
import { SiteHeader } from "../site-header";
import { BreadcrumbJsonLd, ItemListJsonLd, pageMetadata } from "../seo";
export const metadata=pageMetadata("Superbuy Spreadsheet Categories | Shoes, Clothing & Accessories","Browse the independent Superbuy spreadsheet by product category, then verify the live record before ordering.","/categories/");
export default function Categories(){return <main><BreadcrumbJsonLd items={[{name:"Home",path:"/"},{name:"Categories",path:"/categories/"}]}/><ItemListJsonLd name="Superbuy spreadsheet categories" path="/categories/" items={categories.map(x=>({name:x.name,url:`https://kakobuysn.com${x.path}`}))}/><SiteHeader/><section className="directory-shell"><p className="eyebrow">Product directory</p><h1>Browse every category.</h1><p className="directory-intro">Choose a collection to open the matching live catalogue maintained by this site’s publisher. It opens in a new tab. Availability, options and prices can change, so verify the destination record before ordering.</p><div className="category-grid">{categories.map(x=><a className="category-card" href={`https://kakobuysn.com${x.path}`} target="_blank" rel="noopener noreferrer" key={x.name}><span className="category-type">{x.count}</span><h3>{x.name}</h3><p>{x.note}</p><span className="category-link">Open external collection ↗</span></a>)}</div></section></main>}
